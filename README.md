# 📄🔍 Document RAG Search Application

Fully local PDF upload + semantic search app, powered by FAISS, LangChain, and LlamaCpp.

## How to run:
pip install -r requirements.txt
streamlit run app.py